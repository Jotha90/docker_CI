language: minimal

services:
  - docker

before_script:
  - docker --version
  - cd ProyectoFrontend2025

script:
  - docker compose up -d --build
  - sleep 10
  - docker ps

after_script:
  - docker compose down || true